# Modex Ticket Frontend (Vite + React + TypeScript)
## Quickstart
1. Install deps:
   ```
   cd frontend
   npm install
   ```
2. Start dev server:
   ```
   npm run dev
   ```
3. By default frontend expects backend at http://localhost:4000. To change, set VITE_API_BASE in .env.
